# Copie para .env e ajuste, ou use: npm run setup -- --domain=www.seusite.com.br
# IPs com porta usam http no setup (ex.: --domain=203.0.113.10:6173).
# Produção também pode usar .env.production (ver repositório).
VITE_SITE_URL=https://www.exemplo.com.br
